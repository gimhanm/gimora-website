import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'GIMORA - Premium Ceylon Tea & Pepper',
  description: 'Discover GIMORA - Premium Ceylon Black Tea and Black Pepper from Sri Lanka. Authentic, sustainably sourced agricultural products for discerning customers worldwide.',
  keywords: 'Ceylon tea, black pepper, Sri Lanka, premium tea, agricultural exports, sustainable sourcing',
  generator: 'v0.app',
  icons: {
    icon: [
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  colorScheme: 'light',
  themeColor: '#123524',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
